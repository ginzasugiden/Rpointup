# ARCHITECTURE.md — GSDプラットフォーム設計原則

> 本ドキュメントは **GSDアカウント基盤上で動く全サービスに共通する設計原則** を定める。
> ポイント変倍侍（本リポジトリ）は、その原則を説明するための **具体例** として登場する。
> リポジトリ固有の実装事実は [CLAUDE.md](./CLAUDE.md) を参照すること（本書と役割を分ける）。
>
> **表記ルール**
> - 「★現行」… 実コード（`コード.js` / `index.html` / `appsscript.json`）で確認できる事実。
> - 「☆目標」… 刷新で導入する設計方針。まだ実装されていない（推測ではなく「これから決める/作る」もの）。
> - 「（推定）」… 命名や周辺情報からの推定。確定前に要確認。

---

## 1. はじめに

### 1.1 目的
GSDプラットフォームの刷新（ポイント変倍侍を皮切りに、コンテンツページ生成ちゃん等への展開）を進めるにあたり、**サービス横断で守るべき設計原則・データモデル・認証認可・サービス追加手順**を明文化する。個々のサービスがバラバラに設計されることを防ぎ、共通基盤（特に認証マスター）を安全に再利用するための土台とする。

### 1.2 対象読者
- **とみぃ**（プラットフォームオーナー／意思決定者）
- **未来の Claude Code**（本書を文脈として読み、原則に沿った実装・改修を行う）
- **未来の協力エンジニア**（新サービス追加・既存改修の担当者）

### 1.3 改訂履歴の運用方針
- 設計上の決定（原則・データモデル・認証フローの変更）が発生したら、**9章 改訂履歴**に日付と要点を追記する。
- 「☆目標」が実装されたら、該当箇所を「★現行」に書き換え、CLAUDE.md 側に実装事実を反映する。
- 日付は絶対表記（YYYY-MM-DD）で記す。

---

## 2. GSDプラットフォーム概要

### 2.1 GSDアカウント基盤とは
楽天出店店舗を主な利用者とする、複数の業務支援サービスを **単一のアカウント基盤（共通認証マスター）** の上で提供するプラットフォーム。各利用者は1つの **GSDアカウント（`id`）** を持ち、その `id` を鍵にして複数サービスを横断利用する。認証情報・契約状態・課金情報は中央のGoogle Spreadsheetに集約される。

### 2.2 提供サービス一覧（現状）
| サービス | 識別子（想定） | 状態 | 備考 |
|----------|----------------|------|------|
| ポイント変倍侍 | `rpointup` | ★稼働中（本リポジトリ） | 楽天の商品別ポイント変倍を一括設定。店舗別変倍へ拡張予定 |
| コンテンツページ生成ちゃん | `content_gen`（仮） | 稼働中（別管理・本書の直接対象外） | 共通スプレッドシート名の由来。`api_key.CHATGPT_API_KEY` を利用（推定） |
| クーポン発行 | `coupon`（仮） | ☆構想中 | 章立て上の例。仕様未確定 |
| 将来追加予定 | `service_xxx` | ☆未定 | 6章のテンプレートに従って追加 |

> 注: 本リポジトリで実コード確認できるのはポイント変倍侍のみ。他サービスの内部仕様は本書の保証範囲外（とみぃの管理情報に従う）。

### 2.3 システム構成
共通のアーキテクチャパターン（全サービスで踏襲する）:

```
[ブラウザ / 利用者]
      │  HTTPS（fetch, Content-Type: text/plain でプリフライト回避）★現行
      ▼
[フロントエンド] GitHub Pages（静的 index.html、カスタムドメイン=CNAME）★現行
      │  POST {action, id/shopId, password, ...}
      ▼
[バックエンド] Google Apps Script WebApp ★現行
      │  - access: ANYONE_ANONYMOUS（誰でも呼べる＝認証はアプリ層の責務）
      │  - executeAs: USER_DEPLOYING（デプロイ者権限でSpreadsheet/外部APIへアクセス）
      │  - runtime: V8 / timeZone: Asia/Tokyo
      ▼
[データストア] Google Spreadsheet（共通認証マスター + サービス別シート）★現行
      ▼
[外部API] 楽天RMS API（ESA認証）★現行 ／ 将来: Python RPA 等 ☆目標
```

**設計上の含意（重要）**: WebApp は `ANYONE_ANONYMOUS` で公開されるため、Googleログインによる保護は効かない。**本人確認・認可は必ずアプリケーション層（`id` + `password` 照合 → 利用権チェック）で行う**。これが3章・5章の前提。

---

## 3. 設計原則 ★コア★

> 以下は GSDプラットフォーム全サービスが **守るべきルール**（断定形）。

### 3.1 関心の分離: 責務ごとにシートを分ける
1つのシートに複数サービスの都合を詰め込まない。**共通認証は `api_key`、サービス利用権は `account_services`、サービス固有データは `service_xxx`** に分離する。シートの責務は4.1のマトリクスで一意に定める。

### 3.2 既存資産を壊さない: `api_key` シートへの変更を最小化する
`api_key` は全サービスが依存する共通認証マスターである。**新サービス追加のために `api_key` へ列を足してはならない**。サービス固有の項目は `service_xxx` に、利用権は `account_services` に置く。`api_key` の既存列の意味・並び・型を変更しない（読み取りロジックがヘッダー名 `indexOf` に依存するため／★現行 `getShopsData`）。

### 3.3 `id` を全シートの結合キーとする
全シートは `id`（GSDアカウント識別子）を主キー／外部キーとして持つ。サービス間・シート間の突合は必ず `id` で行う。★現行: `api_key.id` がポイント変倍侍の店舗認証キー。

### 3.4 サービスは独立して進化できる
各サービスは自前のリポジトリ・GASプロジェクト・`service_xxx` シートを持ち、**他サービスを巻き込まずにデプロイ・改修できる**。共有するのは `api_key`（認証）と `account_services`（利用権）のみ。

### 3.5 共通基盤の変更は全サービスへの影響を検討してから行う
`api_key` / `account_services` / 認証フローに手を入れる場合は、**全サービスへの波及を確認**し、本書と各 CLAUDE.md を更新してから実装する。共通基盤の破壊的変更は単独サービスの判断で行わない。

---

## 4. データモデル

中央スプレッドシート（現状の物理名は「コンテンツページ生成ちゃん」★現行 = CLAUDE.md記載）に、以下のシートを配置する。

### 4.1 シート責務マトリクス
| シート | 責務 | 所有 | 状態 | 主キー |
|--------|------|------|------|--------|
| `api_key` | 本人認証・楽天API認証・課金/契約状態の共通マスター | 共通基盤 | ★現行 | `id` |
| `account_services` | アカウント×サービスの利用権 | 共通基盤 | ☆新規（未作成） | `id`+`service` |
| `service_rpointup` | ポイント変倍侍 固有設定・機能フラグ | ポイント変倍侍 | ☆新規（未作成） | `id` |
| `service_xxx` | 各サービス固有 | 各サービス | ☆将来 | `id` |

### 4.2 シート定義

#### 4.2.1 `api_key`（既存・共通認証マスター）★現行
ポイント変倍侍の `コード.js getShopsData()` が読むのは **`id, licenseKey, serviceSecret, pw, sid, sname, email` の7列のみ**（ヘッダー名で `indexOf` 参照）。それ以外の列は他サービス／課金基盤が使用する（本リポジトリのコードからは用途を確定できないため一部は推定）。

| 列 | 用途 | 所有/利用 | 確度 |
|----|------|-----------|------|
| `id` | GSDアカウント識別子。全シート結合キー。アプリ認証のユーザーID（楽天店舗ID系を流用） | 共通基盤 | ★現行（`getShopsData`/`authenticateShop`） |
| `CHATGPT_API_KEY` | ChatGPT/OpenAI APIキー（**全ユーザー共通**のGSD社管理キー） | コンテンツページ生成ちゃん | ★事実（とみぃ確認済） |
| `licenseKey` | 楽天RMS ライセンスキー（`BASE64:` 保存） | ポイント変倍侍ほか楽天連携 | ★現行（`getAuthHeader`） |
| `serviceSecret` | 楽天RMS サービスシークレット（`BASE64:` 保存） | ポイント変倍侍ほか楽天連携 | ★現行（`getAuthHeader`） |
| `download` | コンテンツページの残り作成可能数（カウンタ） | コンテンツページ生成ちゃん | ★事実（とみぃ確認済） |
| `pw` | アプリログイン用パスワード（`BASE64:` 保存） | 共通基盤 | ★現行（`authenticateShop`） |
| `sid` | 楽天内部ショップID（数値系） | 共通基盤 | ★現行（`getShopsData`） |
| `sname` | 店舗表示名。認証成功時に `shopName` として返す | 共通基盤 | ★現行（`processPost`） |
| `email` | 連絡先メール。`getShops` 応答に含む | 共通基盤 | ★現行（`processGet`） |
| `flag` | アカウント有効性フラグ。各ツールが必要に応じて参照 | 共通基盤 | ★事実（とみぃ確認済） / 現コードは未チェック |
| `expiry` | 楽天APIキーの有効期限（契約期限ではない） | 楽天連携サービス | ★事実（とみぃ確認済） / 現コードは未チェック |
| `role` | 別サービスで導入された列。本書の直接の管理範囲外（用途は所有サービスを参照） | 別サービス | ★事実（とみぃ確認済） / 本書管理外 |
| `gmo_member_id` | GMOペイメント会員ID | 課金基盤 | 推定 |
| `payment_status` | 支払いステータス | 課金基盤 | 推定 |
| `card_seq` | カード登録連番（GMO） | 課金基盤 | 推定 |
| `card_last4` | カード番号下4桁 | 課金基盤 | 推定 |
| `card_brand` | カードブランド | 課金基盤 | 推定 |
| `card_expire` | カード有効期限 | 課金基盤 | 推定 |
| `next_charge_date` | 次回課金日 | 課金基盤 | 推定 |
| `registered_at` | 登録日時 | 課金基盤 | 推定 |
| `cancelled_at` | 解約日時 | 課金基盤 | 推定 |
| `retry_count` | 課金リトライ回数 | 課金基盤 | 推定 |
| `is_grandfathered` | 旧プラン据置（グランドファザリング）フラグ | 課金基盤 | 推定 |

**不可侵ルール（3.2 の具体化）**:
- 既存列の **名称・意味・型を変更しない**（`getShopsData` がヘッダー名一致に依存）。
- 新サービスのために **列を追加しない**（固有項目は `service_xxx` へ）。
- 認証情報（`pw` / `serviceSecret` / `licenseKey`）は **`BASE64:` プレフィックス付き**で保存する（GAS側でデコード／★現行 5.x 参照）。

#### 4.2.2 `account_services`（新規・サービス利用権）☆目標
アカウントがどのサービスを使えるかを表す。**`api_key` を汚さずに利用権を管理する**ためのシート。

| 列 | 型 | 用途 | 値の例 |
|----|----|------|--------|
| `id` | string | GSDアカウント識別子（`api_key.id` と結合） | `tokyoflower` |
| `service` | string | サービス識別子（スネークケース、6.1） | `rpointup` / `content_gen` / `coupon` |
| `is_active` | boolean | 当該サービスの利用権 有効/無効 | `TRUE` / `FALSE` |
| `role` | string | サービス内権限 | `admin` / `user` |
| `granted_at` | datetime | 利用権付与日時 | `2026-05-24T00:00:00+09:00` |
| `expires_at` | datetime\|空 | 利用権の期限（空=無期限） | `2026-12-31T23:59:59+09:00` |
| `note` | string | 備考 | 任意 |

主キーは `id` + `service` の組（同一アカウントが複数サービス行を持つ）。

> **命名上の注意（同名2列の共存）**: `api_key.role` と本表の `account_services.role` は名前が同じだが、所属シート・責務・所有が異なる別物である。
> - `api_key.role`: 別サービスで導入された列。本書管理外（4.2.1）
> - `account_services.role`: サービス内権限（本書管理）
>
> 現状は両者を区別して扱えば動作上の問題はないが、将来的に運用上の混乱が生じた場合は `account_services.role` を `service_role` 等にリネームすることを検討する。

#### 4.2.3 `service_rpointup`（新規・ポイント変倍侍固有）☆目標
ポイント変倍侍の機能フラグと、店舗別変倍など将来機能に必要な固有情報を持つ。

| 列 | 型 | 用途 |
|----|----|------|
| `id` | string | 結合キー（`api_key.id`） |
| `feature_item_point` | boolean | 商品別ポイント変倍 機能の有効フラグ（★現行の主機能に対応） |
| `feature_shop_point` | boolean | 店舗別ポイント変倍 機能の有効フラグ（☆拡張予定機能） |
| `rms_login_id` | string | 楽天RMS R-Login ID（店舗別変倍等のRPA/ログイン操作用）。要保護 |
| `rms_login_pw` | string | 楽天RMS R-Login パスワード。`BASE64:` 等で保護 |
| `note` | string | 備考 |

> 補足: ★現行の商品別変倍は RMS Item API（`serviceSecret`/`licenseKey`）で実現済み。`rms_login_*` は API では行えない操作（店舗別変倍等）を将来 RPA 等で行うための布石（☆目標）。

#### 4.2.4 将来追加予定の `service_xxx` 群 ☆将来
新サービスごとに `service_<識別子>` シートを作る（例: `service_coupon`）。最低限 `id` 列を持ち、サービス固有の設定・機能フラグを格納する。命名は6.1に従う。

### 4.3 結合キー: `id` を全シート共通の主キーとする
- `api_key.id` を起点に、`account_services`（`id`+`service`）、`service_xxx`（`id`）を `id` で突合する。
- `id` の値は不変とし、楽天店舗ID系を流用する（★現行のアプリ認証ユーザーIDと一致）。

---

## 5. 認証・認可フロー

### 5.1 シーケンス（目標形 ☆ / 現行差分を注記）
```
利用者 → フロント:  id + password + (検索/設定リクエスト)
フロント → GAS:     POST {action, shopId/id, password, ...}（text/plain）★現行
GAS:
  [Layer 0] api_key 照合
     - id 存在確認                                   ★現行
     - password を BASE64 デコードして照合           ★現行
     - serviceSecret / licenseKey の存在確認          ★現行
     - flag / expiry / payment_status の有効性確認     ☆目標（現状 未チェック）
  [Layer 1] account_services で当該 service の利用権   ☆目標（現状 未実装）
     - is_active = TRUE / expires_at 未超過
  [Layer 2] service_xxx で機能フラグ取得               ☆目標（現状 未実装）
     - features = { item_point, shop_point, ... }
GAS → フロント:  { success, shopName, features, ... } ★一部現行（features は ☆目標）
フロント:        features に応じてUIを出し分け        ☆目標（現状 未対応）
```

### 5.2 各レイヤーでのチェック内容
- **Layer 0 — 本人確認 + アカウント有効性（`api_key`）**
  - ★現行: `authenticateShop()` が `id` 一致・`pw`（BASE64デコード）一致・`serviceSecret`/`licenseKey` 存在を確認。
  - ☆目標: `flag`/`expiry`/`payment_status` を見てアカウント自体の有効性を判定。
- **Layer 1 — 利用権（`account_services`）** ☆目標
  - `id`+`service` 行の `is_active=TRUE` かつ `expires_at` 未超過。
- **Layer 2 — 機能フラグ（`service_xxx`）** ☆目標
  - `service_rpointup` の `feature_item_point` / `feature_shop_point` 等を取得。

### 5.3 機能フラグの伝播（features オブジェクト）☆目標
認証成功時、GAS は機能フラグを **`features` オブジェクト**としてフロントに返す。フロントは `features` に応じてUI（タブ・ボタン）を出し分ける。
```json
{ "success": true, "shopName": "…",
  "features": { "item_point": true, "shop_point": false } }
```
> ★現行の応答は `success` / `shopName` / `items` / `totalCount` / `nextCursorMark` / `results[]`（CLAUDE.md「GASレスポンス契約」）。`features` は本設計で追加する。

### 5.4 エラーハンドリング基準
全エラーは ★現行パターン `{ success:false, error:"<メッセージ>" }`（JSON）で返す。フロントは `error` に「認証」を含むか等で表示制御する（★現行 `index.html`）。

| ケース | レイヤー | メッセージ規格 | 状態 |
|--------|----------|----------------|------|
| 認証失敗（id/pw不一致） | L0 | `認証に失敗しました。IDまたはパスワードが正しくありません` | ★現行 |
| API認証情報なし | L0 | `API認証情報が設定されていません` | ★現行 |
| アカウント無効/契約切れ | L0 | `ご契約が無効です。ご契約状況をご確認ください` | ☆目標 |
| 利用権なし | L1 | `このサービスの利用権がありません` | ☆目標 |
| 機能未許可 | L2 | `この機能は現在のプランではご利用いただけません` | ☆目標 |

メッセージは利用者向けに具体的かつ秘密を漏らさない文言とする（どの列で落ちたか等の内部情報は出さない）。

---

## 6. サービス開発テンプレート ★コア★

### 6.1 サービス識別子の決め方
- **英字スネークケース**（小文字・`_` 区切り、例: `rpointup`, `content_gen`, `coupon`）。
- 1つの識別子を、**リポジトリ名・コード内文字列・`account_services.service`・`service_<識別子>` シート名で統一**する。表記ゆれを作らない。

### 6.2 新規サービス立ち上げ手順（チェックリスト）
1. サービス識別子を決定（例: `coupon`）。
2. `account_services.service` で使う値を確定（= 識別子）。
3. `service_<識別子>` タブをスプレッドシートに作成（`id` 列 + 固有列）。
4. リポジトリを作成し、`CLAUDE.md`（固有事実）と本 `ARCHITECTURE.md`（共通設計、参照/配置）を置く。
5. GASプロジェクトを作成し、`clasp clone <scriptId>` でローカル管理化。
6. 認証フローを実装（**5章のパターンに従う**: Layer 0→1→2、`features` 返却）。
7. UIを実装し、`features` に応じた出し分けを行う。
8. デプロイ（7章）。利用者へ `account_services` で利用権を付与。

### 6.3 既存サービス改修手順
1. 影響範囲を確認（共通基盤に触れるか＝3.5 の検討要否）。
2. `ARCHITECTURE.md` / 対象 `CLAUDE.md` を先に更新（設計→実装の順）。
3. 実装 → テスト → デプロイ。共通基盤変更時は全サービスへの波及を確認。

### 6.4 サービス間連携の標準パターン
- 連携は必ず **同じ `id` で結合**する（3.3）。
- 認証情報は **`api_key` を共通参照**する（各サービスで重複保存しない）。サービス固有の認証情報のみ `service_xxx` に持つ。

---

## 7. デプロイと運用
（コマンドの詳細・運用ルールは [CLAUDE.md](./CLAUDE.md) を正とする。ここでは普遍的な手順のみ）

### 7.1 GAS（clasp）
- `clasp pull`（取得・随時可） / `clasp push`（GAS反映・**要確認**） / `clasp deploy`（公開バージョン作成） / `clasp version`（スナップショット）。
- **デプロイ前チェックリスト**: ①ローカル差分の確認 ②`api_key` 等共通基盤への影響有無（3.5）③本番への破壊的変更でないか ④`clasp push` は人手承認後。

### 7.2 GitHub Pages
- `main` への push で即時反映（数十秒〜数分）。★現行（本リポジトリ）。
- カスタムドメインは `CNAME` ファイルで指定（★現行: `pointup.ginzasugiden.com`）。

### 7.3 スプレッドシート運用
- 共通マスター（`api_key` 等）の編集権限は最小限に限定する。
- GAS は `executeAs: USER_DEPLOYING`（★現行）で動くため、**デプロイ者がスプレッドシートにアクセスできること**が前提。デプロイ者を変える際は権限を確認。
- シート編集時は4章の責務・不可侵ルール（3.2）に従う。列の追加・改名は影響調査後に行う。

---

## 8. 用語集
- **GSDアカウント**: プラットフォーム利用者を表す単位。鍵は `id`。
- **`id`**: GSDアカウント識別子。全シート共通の主キー。楽天店舗ID系を流用（★現行のアプリ認証ユーザーID）。
- **`sid`**: 楽天内部のショップID（数値系）。`id` とは別（★現行 `api_key` 列）。
- **BASE64エンコーディング**: `api_key` の認証情報（`pw`/`serviceSecret`/`licenseKey`）の保存形式。値頭に `BASE64:` を付け、GAS が `Utilities.base64Decode` でデコード（★現行）。難読化であり暗号化ではない点に留意。
- **RMS（Rakuten Merchant Server）**: 楽天の店舗運営サーバ。Item API のエンドポイントは `https://api.rms.rakuten.co.jp/es/2.0/...`、認証は `Authorization: ESA base64(serviceSecret:licenseKey)`（★現行）。
- **clasp**: Google Apps Script のCLI。ローカル↔GAS の同期に使う（★現行 v3.x）。
- **features オブジェクト**: 認証成功時にGASが返す機能フラグの集合（☆目標、5.3）。
- **Layer 0/1/2**: 認証認可の段階（本人確認 / 利用権 / 機能フラグ。5章）。

---

## 9. 改訂履歴
- **2026-05-24**: 初版作成。GSDプラットフォームの設計原則・データモデル（`api_key` 現状＋ `account_services` / `service_rpointup` 提案）・3層認証認可・サービス開発テンプレートを定義。ポイント変倍侍を具体例として記述。
- **2026-05-24（同日改訂）**: `api_key` 列定義をとみぃ確認情報で確度向上（`download` / `flag` / `expiry` / `role` / `CHATGPT_API_KEY` の用途を確定）。
- **2026-05-24（同日改訂2）**: 命名上の注意（`role` 同名2列）を 4.2.2 に追記。
